{
  "name": "proof-pr-repo",
  "version": "0.1.29",
  "private": true,
  "description": "ProofPR helps maintainers review evidence, not vibes.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.33.3",
  "keywords": [
    "github-action",
    "pull-request",
    "pull-request-review",
    "pr-triage",
    "code-review",
    "maintainer-tools",
    "open-source",
    "security-tools",
    "sarif",
    "code-scanning",
    "ai-coding",
    "mcp-security"
  ],
  "homepage": "https://github.com/linsk27/proof-pr#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/linsk27/proof-pr.git"
  },
  "bugs": {
    "url": "https://github.com/linsk27/proof-pr/issues"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm --filter @proof-pr/core build && pnpm -r typecheck",
    "benchmark": "pnpm --filter @proof-pr/core build && pnpm --filter proof-pr build && node packages/cli/dist/index.js benchmark --cases benchmarks/cases",
    "benchmark:report": "pnpm --filter @proof-pr/core build && pnpm --filter proof-pr build && node packages/cli/dist/index.js benchmark --cases benchmarks/cases --format markdown --output benchmarks/report.md",
    "docs:screenshots": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/render-doc-screenshots.ps1",
    "pack:cli": "pnpm --filter proof-pr exec npm pack --dry-run",
    "release:check": "pnpm typecheck && pnpm test && pnpm build && pnpm benchmark && pnpm pack:cli"
  },
  "devDependencies": {
    "@types/node": "^20.19.24",
    "@types/picomatch": "^3.0.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
